<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.pms.mapper.BrandMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, first_letter, sort, factory_status, show_status, product_count, product_comment_count, logo, big_pic, brand_story
    </sql>
    <update id="updateBatchShowStatus">
        UPDATE	pms_brand
        SET	show_status=#{param2}
        WHERE
        id IN
        <foreach item="item" index="index" collection="ids" separator="," open="(" close=")">
            #{item.id}
        </foreach>
    </update>
    <update id="updateBatchFactoryStatus">
        UPDATE	pms_brand
        SET	factory_status=#{param2}
        WHERE
        id IN
        <foreach item="item" index="index" collection="ids" separator="," open="(" close=")">
            #{item.id}
        </foreach>
    </update>

</mapper>
